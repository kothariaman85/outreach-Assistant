
GROQ_API_KEY='WRITE_YOUR_API_KEY'
PORT=3001

# Email sending (used by /api/send-email)
# For Gmail: use smtp.gmail.com, port 587, and a 16-character App Password
# (not your normal password) generated at https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=EMAIL_ADDRESS
SMTP_PASS=16 digit password from google to send email goto myaccount.google.com/apppasswords setup a app then you get a 16 digit password then paste it here
FROM_NAME= choose name

